<br/>

<%= form_for :email do |form| %>

  <%= render(:partial => "field",
             :collection => @email.fields,  # TODO: SORT THEM!
             :locals => { :form => form }) %>

  <%= hidden_field(:email, :class) %>

  <%= form.submit "Send" %>

<% end %>
